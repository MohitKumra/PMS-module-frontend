/**
 * tailwind.config.ts — Tailwind v4
 *
 * In Tailwind v4 with @tailwindcss/vite, the config file is optional.
 * Theme tokens (colors, radii) are declared via @theme inline in globals.css.
 * Dark mode is handled by [data-theme='dark'] attribute toggling in platform/theme.ts.
 *
 * This file is kept for reference only — content scanning and dark mode
 * variant are configured automatically by @tailwindcss/vite in v4.
 */
export default {};